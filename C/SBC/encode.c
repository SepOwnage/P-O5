#include "encode.h"
struct chunk{
	*left_odd
	*left_even
	*right_odd
	*right_even

}
void encode(short buffer[BUFFERSIZE],struct chunk *previous_chunk){
	newchunk=new chunk()	
	separateInFour(buffer,newchunk);
	putValuesInChunk;
	convolve(newchunk->left_odd,previous_chunk->left_odd,filterlength,chunk);

	
}


